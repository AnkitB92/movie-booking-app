```
                  movie-booking-app
        ___________________|_______________
        |                 |               |
  (data folder)     (Django app)     (React app)


DJANGO:
Setup virtual environment on desktop:
  > create virtual environment on desktop: py -m venv myEnv
  > activate virtual environment  ..desktop > myEnv\Scripts\activate.bat

Install dependencies for django project in virtual environment
  > Install dependencies: pip install -r requirements.txt
  > Migrate: py manage.py migrate
  > import JSON files to mongodb after migrate

REACT SETUP:
1> Install dependencies: npm install while in movie_booking_react folder
```
